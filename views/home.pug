extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each item in videos
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            //- h1=item.title
            //- p=item.description
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })
            +videoBlock({
                id: item.id,
                fileUrl: item.fileUrl,
                title: item.title,
                views: item.views
            })