extends layouts/main

block content
    .form-container
        form(action=routes.changePassword, method="post")
            input(type="password", placeholder="Old password",name="password0")
            input(type="password", placeholder="New password",name="password1")
            input(type="password", placeholder="Verify new password",name="password2")
            input(type="submit", value="Change Password")